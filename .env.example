DATABASE_URL=postgresql+psycopg2://postgres:123@localhost:5432/testdb
SECRET_KEY=change-me-to-something-random-and-long
SQL_ECHO=false

# Публичный URL приложения (используется в ссылках в письмах)
APP_BASE_URL=http://127.0.0.1:8000

# Email. Если EMAIL_SMTP_HOST пустой — письма печатаются в консоль (удобно для dev).
EMAIL_FROM=no-reply@calendarproject.local
EMAIL_SMTP_HOST=
EMAIL_SMTP_PORT=587
EMAIL_SMTP_USER=
EMAIL_SMTP_PASSWORD=
